# VocabVault runtime configuration example.
# This app uses a local dictionary and does not require external AI provider keys.

# Public base URL if you deploy the app (optional).
APP_URL="http://localhost:3000"

# Server port.
PORT=3000
